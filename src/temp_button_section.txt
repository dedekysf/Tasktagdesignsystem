              <div className="space-y-8">
                {/* 1. Rectangular Button - Default Border Radius */}
                <div>
                  <h3 className="mb-4">Rectangular Button (8px radius)</h3>
                  <div className="component-card">
                    <div className="space-y-6">
                      {['xl', 'lg', 'md', 'sm', 'xs'].map((size) => (
                        <div key={size}>
                          <p className="text-web-metadata-primary text-metadata mb-3">{size.toUpperCase()}</p>
                          <div className="flex flex-wrap gap-3 items-center">
                            <Button size={size as ButtonSize} variant="fill" className="bg-[var(--black)] text-[var(--white)]">
                              Button
                            </Button>
                            <Button size={size as ButtonSize} variant="fill" className="bg-[var(--black)] text-[var(--white)]">
                              <ChevronLeft size={size === 'xl' ? 20 : size === 'lg' ? 18 : size === 'md' ? 18 : size === 'sm' ? 16 : 14} />
                              Left Icon
                            </Button>
                            <Button size={size as ButtonSize} variant="fill" className="bg-[var(--black)] text-[var(--white)]">
                              Right Icon
                              <ChevronRight size={size === 'xl' ? 20 : size === 'lg' ? 18 : size === 'md' ? 18 : size === 'sm' ? 16 : 14} />
                            </Button>
                            <Button size={size as ButtonSize} variant="fill" className="bg-[var(--black)] text-[var(--white)]">
                              <User size={size === 'xl' ? 20 : size === 'lg' ? 18 : size === 'md' ? 18 : size === 'sm' ? 16 : 14} />
                            </Button>
                          </div>
                        </div>
                      ))}
                    </div>
                  </div>
                  <div className="mt-4">
                    <CodeExample
                      title="Usage Example"
                      code={`import { Button } from './components';
import { ChevronLeft, ChevronRight, User } from 'lucide-react';

// Sizes
<Button size="xl" variant="fill" className="bg-[var(--black)] text-[var(--white)]">
  Button
</Button>
<Button size="lg" variant="fill" className="bg-[var(--black)] text-[var(--white)]">
  Button
</Button>
<Button size="md" variant="fill" className="bg-[var(--black)] text-[var(--white)]">
  Button
</Button>
<Button size="sm" variant="fill" className="bg-[var(--black)] text-[var(--white)]">
  Button
</Button>
<Button size="xs" variant="fill" className="bg-[var(--black)] text-[var(--white)]">
  Button
</Button>

// With left icon
<Button size="md" variant="fill" className="bg-[var(--black)] text-[var(--white)]">
  <ChevronLeft size={18} />
  Left Icon
</Button>

// With right icon
<Button size="md" variant="fill" className="bg-[var(--black)] text-[var(--white)]">
  Right Icon
  <ChevronRight size={18} />
</Button>

// Icon only
<Button size="md" variant="fill" className="bg-[var(--black)] text-[var(--white)]">
  <User size={18} />
</Button>`}
                    />
                  </div>
                </div>

                {/* 2. Rounded Button - 16px Border Radius */}
                <div>
                  <h3 className="mb-4">Rounded Button (16px radius)</h3>
                  <div className="component-card">
                    <div className="space-y-6">
                      {['xl', 'lg', 'md', 'sm', 'xs'].map((size) => (
                        <div key={size}>
                          <p className="text-web-metadata-primary text-metadata mb-3">{size.toUpperCase()}</p>
                          <div className="flex flex-wrap gap-3 items-center">
                            <Button size={size as ButtonSize} variant="fill" className="bg-[var(--black)] text-[var(--white)]" style={{ borderRadius: 'var(--radius-16)' }}>
                              Button
                            </Button>
                            <Button size={size as ButtonSize} variant="fill" className="bg-[var(--black)] text-[var(--white)]" style={{ borderRadius: 'var(--radius-16)' }}>
                              <ChevronLeft size={size === 'xl' ? 20 : size === 'lg' ? 18 : size === 'md' ? 18 : size === 'sm' ? 16 : 14} />
                              Left Icon
                            </Button>
                            <Button size={size as ButtonSize} variant="fill" className="bg-[var(--black)] text-[var(--white)]" style={{ borderRadius: 'var(--radius-16)' }}>
                              Right Icon
                              <ChevronRight size={size === 'xl' ? 20 : size === 'lg' ? 18 : size === 'md' ? 18 : size === 'sm' ? 16 : 14} />
                            </Button>
                            <Button size={size as ButtonSize} variant="fill" className="bg-[var(--black)] text-[var(--white)]" style={{ borderRadius: 'var(--radius-16)' }}>
                              <User size={size === 'xl' ? 20 : size === 'lg' ? 18 : size === 'md' ? 18 : size === 'sm' ? 16 : 14} />
                            </Button>
                          </div>
                        </div>
                      ))}
                    </div>
                  </div>
                  <div className="mt-4">
                    <CodeExample
                      title="Usage Example"
                      code={`import { Button } from './components';
import { ChevronLeft, ChevronRight, User } from 'lucide-react';

// Sizes with 16px border radius
<Button 
  size="xl" 
  variant="fill" 
  className="bg-[var(--black)] text-[var(--white)]"
  style={{ borderRadius: 'var(--radius-16)' }}
>
  Button
</Button>

<Button 
  size="lg" 
  variant="fill" 
  className="bg-[var(--black)] text-[var(--white)]"
  style={{ borderRadius: 'var(--radius-16)' }}
>
  Button
</Button>

<Button 
  size="md" 
  variant="fill" 
  className="bg-[var(--black)] text-[var(--white)]"
  style={{ borderRadius: 'var(--radius-16)' }}
>
  Button
</Button>

<Button 
  size="sm" 
  variant="fill" 
  className="bg-[var(--black)] text-[var(--white)]"
  style={{ borderRadius: 'var(--radius-16)' }}
>
  Button
</Button>

<Button 
  size="xs" 
  variant="fill" 
  className="bg-[var(--black)] text-[var(--white)]"
  style={{ borderRadius: 'var(--radius-16)' }}
>
  Button
</Button>

// With left icon
<Button 
  size="md" 
  variant="fill" 
  className="bg-[var(--black)] text-[var(--white)]"
  style={{ borderRadius: 'var(--radius-16)' }}
>
  <ChevronLeft size={18} />
  Left Icon
</Button>

// With right icon
<Button 
  size="md" 
  variant="fill" 
  className="bg-[var(--black)] text-[var(--white)]"
  style={{ borderRadius: 'var(--radius-16)' }}
>
  Right Icon
  <ChevronRight size={18} />
</Button>

// Icon only
<Button 
  size="md" 
  variant="fill" 
  className="bg-[var(--black)] text-[var(--white)]"
  style={{ borderRadius: 'var(--radius-16)' }}
>
  <User size={18} />
</Button>`}
                    />
                  </div>
                </div>

                {/* 3. Button Variants */}
                <div>
                  <h3 className="mb-4">Button Variants</h3>
                  <div className="component-card">
                    <div className="space-y-6">
                      {/* Fill Variant */}
                      <div>
                        <p className="text-web-metadata-primary text-metadata mb-3">FILL</p>
                        <div className="flex flex-wrap gap-3 items-center">
                          <Button variant="fill">Primary</Button>
                          <Button variant="fill" className="bg-[var(--black)] text-[var(--white)]">Black</Button>
                          <Button variant="fill" className="bg-[var(--blue)] text-[var(--white)]">Blue</Button>
                          <Button variant="fill" className="bg-[var(--purple)] text-[var(--white)]">Purple</Button>
                          <Button variant="fill" disabled>Disabled</Button>
                        </div>
                      </div>

                      {/* Outline Variant */}
                      <div>
                        <p className="text-web-metadata-primary text-metadata mb-3">OUTLINE</p>
                        <div className="flex flex-wrap gap-3 items-center">
                          <Button variant="outline">Primary</Button>
                          <Button variant="outline" className="border-[var(--black)] text-[var(--black)]">Black</Button>
                          <Button variant="outline" className="border-[var(--blue)] text-[var(--blue)]">Blue</Button>
                          <Button variant="outline" className="border-[var(--purple)] text-[var(--purple)]">Purple</Button>
                          <Button variant="outline" disabled>Disabled</Button>
                        </div>
                      </div>

                      {/* Ghost Variant */}
                      <div>
                        <p className="text-web-metadata-primary text-metadata mb-3">GHOST</p>
                        <div className="flex flex-wrap gap-3 items-center">
                          <Button variant="ghost">Primary</Button>
                          <Button variant="ghost" className="text-[var(--black)]">Black</Button>
                          <Button variant="ghost" className="text-[var(--blue)]">Blue</Button>
                          <Button variant="ghost" className="text-[var(--purple)]">Purple</Button>
                          <Button variant="ghost" disabled>Disabled</Button>
                        </div>
                      </div>

                      {/* Destructive Variant */}
                      <div>
                        <p className="text-web-metadata-primary text-metadata mb-3">DESTRUCTIVE</p>
                        <div className="flex flex-wrap gap-3 items-center">
                          <Button variant="destructive">Delete</Button>
                          <Button variant="destructive">
                            <Trash2 size={18} />
                            Remove
                          </Button>
                          <Button variant="destructive">
                            <Trash2 size={18} />
                          </Button>
                          <Button variant="destructive" disabled>Disabled</Button>
                        </div>
                      </div>
                    </div>
                  </div>
                  <div className="mt-4">
                    <CodeExample
                      title="Usage Example"
                      code={`import { Button } from './components';
import { Trash2 } from 'lucide-react';

// FILL VARIANT - Solid background
<Button variant="fill">Primary</Button>
<Button variant="fill" className="bg-[var(--black)] text-[var(--white)]">
  Black
</Button>
<Button variant="fill" className="bg-[var(--blue)] text-[var(--white)]">
  Blue
</Button>
<Button variant="fill" disabled>Disabled</Button>

// OUTLINE VARIANT - Transparent with border
<Button variant="outline">Primary</Button>
<Button variant="outline" className="border-[var(--black)] text-[var(--black)]">
  Black
</Button>
<Button variant="outline" className="border-[var(--blue)] text-[var(--blue)]">
  Blue
</Button>
<Button variant="outline" disabled>Disabled</Button>

// GHOST VARIANT - Transparent without border
<Button variant="ghost">Primary</Button>
<Button variant="ghost" className="text-[var(--black)]">
  Black
</Button>
<Button variant="ghost" className="text-[var(--blue)]">
  Blue
</Button>
<Button variant="ghost" disabled>Disabled</Button>

// DESTRUCTIVE VARIANT - For delete/remove actions
<Button variant="destructive">Delete</Button>
<Button variant="destructive">
  <Trash2 size={18} />
  Remove
</Button>
<Button variant="destructive" disabled>Disabled</Button>`}
                    />
                  </div>
                </div>

              </div>
