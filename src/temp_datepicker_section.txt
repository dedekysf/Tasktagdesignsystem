
          {/* Datepicker Component */}
          {activeSection === 'datepicker' && (
            <section>
              <SectionHeader
                icon={LayoutGrid}
                title="Mobile Date Range"
                description="Total Variant: 2"
              />

              {/* Section 1: Basic (Without Label) */}
              <div style={{ marginBottom: 'var(--spacing-56)' }}>
                <h3 style={{ fontWeight: 'var(--font-weight-semibold)', marginBottom: 'var(--spacing-8)' }}>Basic</h3>
                <TabsContainer
                  activeTab={datepickerBasicTab}
                  onTabChange={setDatepickerBasicTab}
                  tabs={[
                    { value: 'preview', label: 'Preview' },
                    { value: 'usage', label: 'Usage' }
                  ]}
                >
                  <TabPanel value="preview" activeTab={datepickerBasicTab}>
                    <div className="component-card">
                      <div style={{ maxWidth: '375px' }}>
                        <Datepicker
                          variant="basic"
                          size="md"
                          placeholder="Select date"
                          value={selectedDate}
                          onChange={setSelectedDate}
                        />
                      </div>
                    </div>
                  </TabPanel>
                  <TabPanel value="usage" activeTab={datepickerBasicTab}>
                    <CodeExample
                      title="Basic Datepicker"
                      code={`import { Datepicker } from './components';

<Datepicker
  variant="basic"
  size="md"
  placeholder="Select date"
  value={selectedDate}
  onChange={setSelectedDate}
/>`}
                    />
                  </TabPanel>
                </TabsContainer>
              </div>

              {/* Section 2: With Label */}
              <div style={{ marginBottom: 'var(--spacing-56)' }}>
                <h3 style={{ fontWeight: 'var(--font-weight-semibold)', marginBottom: 'var(--spacing-8)' }}>With Label</h3>
                <TabsContainer
                  activeTab={datepickerLabelTab}
                  onTabChange={setDatepickerLabelTab}
                  tabs={[
                    { value: 'preview', label: 'Preview' },
                    { value: 'usage', label: 'Usage' }
                  ]}
                >
                  <TabPanel value="preview" activeTab={datepickerLabelTab}>
                    <div className="component-card">
                      <div style={{ maxWidth: '375px' }}>
                        <Datepicker
                          variant="with-label"
                          size="md"
                          label="Due Date"
                          placeholder="Select date"
                          value={selectedDateWithLabel}
                          onChange={setSelectedDateWithLabel}
                        />
                      </div>
                    </div>
                  </TabPanel>
                  <TabPanel value="usage" activeTab={datepickerLabelTab}>
                    <CodeExample
                      title="Datepicker with Label"
                      code={`import { Datepicker } from './components';

<Datepicker
  variant="with-label"
  size="md"
  label="Due Date"
  placeholder="Select date"
  value={selectedDate}
  onChange={setSelectedDate}
/>`}
                    />
                  </TabPanel>
                </TabsContainer>
              </div>

              {/* Section 3: Sizes */}
              <div style={{ marginBottom: 'var(--spacing-56)' }}>
                <h3 style={{ fontWeight: 'var(--font-weight-semibold)', marginBottom: 'var(--spacing-8)' }}>Sizes</h3>
                <TabsContainer
                  activeTab={datepickerSizeTab}
                  onTabChange={setDatepickerSizeTab}
                  tabs={[
                    { value: 'preview', label: 'Preview' },
                    { value: 'usage', label: 'Usage' }
                  ]}
                >
                  <TabPanel value="preview" activeTab={datepickerSizeTab}>
                    <div className="component-card">
                      <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--spacing-16)', maxWidth: '375px' }}>
                        <div>
                          <p className="text-web-metadata-primary text-metadata" style={{ marginBottom: 'var(--spacing-8)', color: 'var(--text-secondary)' }}>Small (32px)</p>
                          <Datepicker
                            variant="basic"
                            size="sm"
                            placeholder="Select date"
                          />
                        </div>
                        <div>
                          <p className="text-web-metadata-primary text-metadata" style={{ marginBottom: 'var(--spacing-8)', color: 'var(--text-secondary)' }}>Medium (40px)</p>
                          <Datepicker
                            variant="basic"
                            size="md"
                            placeholder="Select date"
                          />
                        </div>
                      </div>
                    </div>
                  </TabPanel>
                  <TabPanel value="usage" activeTab={datepickerSizeTab}>
                    <CodeExample
                      title="Datepicker Sizes"
                      code={`import { Datepicker } from './components';

// Small (32px)
<Datepicker
  variant="basic"
  size="sm"
  placeholder="Select date"
/>

// Medium (40px)
<Datepicker
  variant="basic"
  size="md"
  placeholder="Select date"
/>`}
                    />
                  </TabPanel>
                </TabsContainer>
              </div>

              {/* Section 4: States */}
              <div style={{ marginBottom: 'var(--spacing-56)' }}>
                <h3 style={{ fontWeight: 'var(--font-weight-semibold)', marginBottom: 'var(--spacing-8)' }}>States</h3>
                <TabsContainer
                  activeTab={datepickerStatesTab}
                  onTabChange={setDatepickerStatesTab}
                  tabs={[
                    { value: 'preview', label: 'Preview' },
                    { value: 'usage', label: 'Usage' }
                  ]}
                >
                  <TabPanel value="preview" activeTab={datepickerStatesTab}>
                    <div className="component-card">
                      <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--spacing-16)', maxWidth: '375px' }}>
                        <div>
                          <p className="text-web-metadata-primary text-metadata" style={{ marginBottom: 'var(--spacing-8)', color: 'var(--text-secondary)' }}>Default</p>
                          <Datepicker
                            variant="basic"
                            size="md"
                            placeholder="Select date"
                          />
                        </div>
                        <div>
                          <p className="text-web-metadata-primary text-metadata" style={{ marginBottom: 'var(--spacing-8)', color: 'var(--text-secondary)' }}>Error</p>
                          <Datepicker
                            variant="basic"
                            size="md"
                            placeholder="Select date"
                            error={true}
                          />
                        </div>
                        <div>
                          <p className="text-web-metadata-primary text-metadata" style={{ marginBottom: 'var(--spacing-8)', color: 'var(--text-secondary)' }}>Disabled</p>
                          <Datepicker
                            variant="basic"
                            size="md"
                            placeholder="Select date"
                            disabled={true}
                          />
                        </div>
                      </div>
                    </div>
                  </TabPanel>
                  <TabPanel value="usage" activeTab={datepickerStatesTab}>
                    <CodeExample
                      title="Datepicker States"
                      code={`import { Datepicker } from './components';

// Default
<Datepicker
  variant="basic"
  size="md"
  placeholder="Select date"
/>

// Error
<Datepicker
  variant="basic"
  size="md"
  placeholder="Select date"
  error={true}
/>

// Disabled
<Datepicker
  variant="basic"
  size="md"
  placeholder="Select date"
  disabled={true}
/>`}
                    />
                  </TabPanel>
                </TabsContainer>
              </div>
            </section>
          )}
