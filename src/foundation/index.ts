/**
 * TaskTag Design System v3 - Foundation
 * 
 * Central export file for all design foundation tokens.
 * 
 * Usage Examples:
 * - import { colors, spacing, radius } from './foundation';
 * - import { typography, elevation } from './foundation';
 */

export { colors, typography, spacing, radius, elevation, borderWidth } from './tokens';
